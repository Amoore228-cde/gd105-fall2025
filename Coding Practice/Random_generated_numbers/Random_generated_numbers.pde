void setup () {
// generate random floating point number 
float value = random(10, 100);
// prints the random number in the console
// convert to integer number int position = (round value);
// or: the same as above in one step int position = round(random, 100));
}
